# Optional. Set this to call the API directly instead of going through the dev
# proxy - the app then uses absolute URLs and relies on CORS. Leave unset to
# keep requests same-origin, which is how production serves them.
# VITE_API_BASE_URL=http://localhost:8000
